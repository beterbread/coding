#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int t; cin >> t;
    while (t--) {
        int n; cin >> n;
        long long c; cin >> c;
        vector<long long> a(n);
        for (long long& i : a) cin >> i;

    }
}