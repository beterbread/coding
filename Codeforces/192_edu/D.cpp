#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int t; cin >> t;
    int test = 0;
    while (t--) {
        string a,b; cin >> a >> b;
        int n = a.size(), m = b.size();
        
    }
}
